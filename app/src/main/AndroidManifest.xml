<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:icon="@mipmap/icon"
        android:roundIcon="@mipmap/icon_round"
        android:theme="@style/Theme.PickaDish">
        <activity
            android:name=".feature.favoritefood.FavoriteFoodActivity"
            android:exported="false"
            android:label="@string/title_activity_favorite_food"
            android:theme="@style/Theme.PickaDish" />
        <activity
            android:name=".feature.fooddetail.FoodDetailActivity"
            android:exported="false"
            android:label="@string/title_activity_food_detail"
            android:theme="@style/Theme.PickaDish" />
        <activity
            android:name=".feature.home.HomeActivity"
            android:exported="true"
            android:theme="@style/Theme.PickaDish">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>